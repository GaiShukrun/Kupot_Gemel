pipeline {
    agent any

    environment {
        DOCKER_FRONTEND_IMAGE = "vladfu/test-docker-front"
        DOCKER_BACKEND_IMAGE = "vladfu/test-docker-back"
        GIT_URL = "https://github.com/GaiShukrun/Kupot_Gemel.git"  
    }

    stages {
        stage('Checkout') {
            steps {
                git url: "${GIT_URL}", branch: 'main'
            }
        }

        stage('Build') {
            parallel {
                stage('Build Frontend') {
                    steps {
                        dir('.') {
                            sh "docker build -t ${DOCKER_FRONTEND_IMAGE}:${latest} ."
                        }
                    }
                }
                stage('Build Backend') {
                    steps {
                        dir('MongoDB') {
                            sh "docker build -t ${DOCKER_BACKEND_IMAGE}:${latest} ."
                        }
                    }
                }
            }
        }

        stage('Tests') {
            steps {
                dir('.') {
                    sh "docker run --rm ${DOCKER_FRONTEND_IMAGE}:${latest} npm test"
                }
            }
        }

        stage('Clean up') {
            steps {
                sh "docker rmi ${DOCKER_FRONTEND_IMAGE}:${latest}"
                sh "docker rmi ${DOCKER_BACKEND_IMAGE}:${latest}"
            }
        }
    }

    post {
        always {
            sh 'docker image prune -f'
        }
    }
}
